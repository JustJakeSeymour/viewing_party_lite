<h3>Movie Results</h3>

<% @movies.each do |movie| %>
  <div id='left'><%= link_to movie[:title], user_movie_path(@user, movie[:id]) %></div>
  <div id='right'><%= movie[:vote_average] %></div>
<% end %>